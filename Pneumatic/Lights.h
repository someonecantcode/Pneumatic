class Lights{
    public:
       void LightON(int port);
       void LightOFF(int port);
       void OrderON(int lights[], int size, int delay);
       void OrderOFF(int lights[], int size, int delay);
};
